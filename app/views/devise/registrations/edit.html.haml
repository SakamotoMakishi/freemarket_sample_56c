=render 'layouts/header'
- breadcrumb :mypage
= render "layouts/breadcrumbs"
.mypage
  .mypage__nav
    = render 'layouts/mypage-sidebar'
    .mypage__nav__main-contents
      .mypage__nav__main-contents__mypage-identification
        %h2.mypage__nav__main-contents__mypage-identification__head メール/パスワード
      = form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f|
        .edit-user
          .edit-user__field
            = f.label :メールアドレス
            %br/
            = f.email_field :email, autofocus: true, autocomplete: "email" ,class:'input_box'
            %p メールアドレスを変更すると確認メールが送信されます。メール内のURLをクリックすると変更完了です。
          - if devise_mapping.confirmable? && resource.pending_reconfirmation?
            %div
              Currently waiting confirmation for: #{resource.unconfirmed_email}
          .edit-user__fields
            .edit-user__fields__field
              = f.label :現在のパスワード
              = f.password_field :current_password, autocomplete: "current-password", class:'input_box'
            .edit-user__fields__field
              = f.label :新しいパスワード
              = f.password_field :password, autocomplete: "new-password" ,class:'input_box',placeholder:'７文字以上'
            .edit-user__fields__field
              = f.label :新しいパスワード確認
              %br/
              = f.password_field :password_confirmation, autocomplete: "new-password", class:'input_box',placeholder:'７文字以上'
              %p パスワードを設定したい場合は上記をすべて入力してください。（設定しない場合は空欄）
            .edit-user__fields__actions
              = f.submit "変更する", class: "submit_btn"